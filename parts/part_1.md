# Parte 1 — Dependências

## Objetivo

Instalar as dependências necessárias.

## Passos

- Adicionar `flame` no `pubspec.yaml`.
- Adicionar `flame_audio` no `pubspec.yaml`.
- Executar `flutter pub get`.

## Resultado esperado

Dependências instaladas e projeto pronto para iniciar o jogo.
