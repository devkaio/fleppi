# Parte 2 — Inicialização do jogo

## Objetivo

Criar a classe `FleppiGame` e iniciar o jogo no `runApp`.

## Passos

- Criar `FleppiGame`.
- Usar `GameWidget` no `runApp`.

## Resultado esperado

App iniciando o jogo com `FleppiGame`.
